<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pj1.mapper.MemberMapper">
	<select id="selectMemberList" resultType="pj1.dto.MemberDto">
	select * from member where mem_deleted_yn = 'N' order by mem_idx desc 

	</select>
	
	<select id="selectDetailMember" resultType="pj1.dto.MemberDto">
	select * from member where mem_deleted_yn = 'N' and mem_email = #{memEmail}
	</select>
	
	<insert id="insertMember" parameterType="pj1.dto.MemberDto">
		insert into
		member(mem_name,mem_phone,mem_postnum,mem_addr1,mem_addr2,mem_email,mem_pw)
		values(#{memName},#{memPhone},#{memPostNum},#{memAddr1},#{memAddr2},#{memEmail},#{memPw})

		<selectKey keyProperty="memIdx" resultType="Integer">
			select last_insert_id()
		</selectKey>
	</insert>
	<select id="login" parameterType="pj1.dto.MemberDto" resultType="pj1.dto.MemberDto">
	select * from member
	where mem_email = #{memEmail} and mem_pw = #{memPw}
	</select>
	
</mapper>



<!-- <select id="selectMemberList" resultType="board.dto.MemberDto">
		select member_seq, member_name, member_email
		     , date_format(created_datetime, '%Y.%m.%d %H:%i:%s') as created_datetime
		  from t_member
		 where deleted_yn = 'N'
		 order by member_seq desc 
	</select> -->
